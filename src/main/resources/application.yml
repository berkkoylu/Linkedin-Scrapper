



spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/linkedin_scrapperDb
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        user_sql_comments: true
  main:
    banner-mode: off